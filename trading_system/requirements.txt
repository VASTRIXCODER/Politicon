# --- core ---
pandas>=2.0
numpy>=1.24
python-dotenv>=1.0
requests>=2.28          # live frontier feeds: DexScreener (crypto) + Polymarket

# --- broker (Alpaca, official SDK) ---
alpaca-py>=0.21

# --- market data ---
yfinance>=0.2.40

# --- terminal dashboard ---
rich>=13.0

# --- web dashboard (signals-only mode) ---
flask>=3.0

# --- backtest charts ---
matplotlib>=3.7

# --- optional: Claude AI briefing layer (only needed if AI_BRIEFING=true) ---
anthropic>=0.40

# --- optional: crypto via Coinbase Advanced Trade (only needed if BROKER=coinbase) ---
# coinbase-advanced-py>=1.2

# --- optional: live server-side TradingView screener (market_extras.py, /api/movers) ---
# tradingview-screener>=0.5

# --- optional: tests ---
# pytest>=7.0
